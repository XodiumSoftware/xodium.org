# Improvement Backlog

## High Impact / Low Effort
[ ] 1. Add `prefers-reduced-motion` fallback for parallax, wireframes, and team deck.
[ ] 2. Bake GitHub repos/members JSON at build time; load instantly, refresh silently.
[ ] 3. Self-host `og:image` instead of pointing to Gist.
[ ] 4. Fix CLS by adding explicit `width`/`height` to avatar images.
[ ] 5. Fix logo hover animation so it stops on mobile after a brief timer.

## High Impact / Medium Effort
[ ] 6. Insert an About/Mission section between hero and projects.
[ ] 7. Scope `Enter`/`Space` keyboard listeners to the team deck container.
[ ] 8. Add lightweight privacy-first analytics (Cloudflare / Plausible).

## Architecture & DX
[x] 9. Replace defensive `web_sys` `.unwrap()` calls with `if let` fallbacks.
[x] 10. Wire `wasm-pack test --headless` into GitHub Actions CI.
[x] 11. Add WASM size budget check in CI (e.g., fail if > 500 KB).
[ ] 12. Evaluate `leptos_i18n` scaffolding for future internationalization.
[ ] 13. Add a minimal service worker for offline asset caching.
