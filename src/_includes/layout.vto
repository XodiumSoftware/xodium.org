<!DOCTYPE html>
<html lang="{{ lang }}">
  <head>
    <meta charset="{{ charset }}" />
    <title>{{ title }}</title>
    <link rel="icon" href="{{ favicon }}" />
  </head>
  <body>
    {{ content }}
  </body>
</html>
